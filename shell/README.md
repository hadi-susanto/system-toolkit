# 🐚 SysKit Shell Integration Assets

This directory will store Bash and Zsh integration files used by the SysKit
shell toolkit.

The toolkit will use these files as the source for shell integrations installed
and activated in their designated locations. Shared shell command routing and
implementation live under `lib/shell/`.
